#!/bin/bash -l
#PBS -l walltime=12:00:00
#PBS -l select=1:ncpus=12:mem=60G
#PBS -o outanderror
#PBS -e outanderror

cd $PBS_O_WORKDIR
cd ../

module load r/3.6.1-foss-2019a

export MC_CORES=4
export OMP_NUM_THREADS=1

R CMD BATCH --slave ${path} ${log_path}_${indexN}_${indexB}_${indexU}_${indexE}_${PBS_ARRAY_INDEX}.log
